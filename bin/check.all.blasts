#!/usr/bin/perl -w
use strict;

my ($f) = @ARGV;
if ((not defined $f)) {
    print "\nUsage: $0 <file with list of projs>\n";
    exit 0;
}

my $cmd = "foreach.line.in.file.do.cmd $f check.blasts.pl";
 print "$cmd\n";
system($cmd);
if ( $? ) { die "Command failed: $cmd: $!"; }
